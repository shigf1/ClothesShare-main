<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClothesShare"
        tools:targetApi="31">

        <!-- MainActivity is the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <!-- This makes the activity the main entry point -->
                <action android:name="android.intent.action.MAIN" />
                <!-- This makes the activity appear in the app launcher -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true" />

        <!-- MessagesActivity -->
        <activity
            android:name=".MessagesActivity"
            android:exported="true" />

        <!-- UploadActivity -->
        <activity
            android:name=".UploadActivity"
            android:exported="true" />

        <!-- ExperienceActivity -->
        <activity
            android:name=".ExperienceActivity"
            android:exported="true" />


    </application>

</manifest>